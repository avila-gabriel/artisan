gleam run -m choire
> to update all dependencies


the system resolves around a single object, the bought, which has a list of products. so most fields are part of the product, only some are for the whole thing.

object lifecycle: sales_intake → purchasing → receiving → delivery

plus a management client that can alter at any point and have the complete view
plus a sales client that can have a limited view
