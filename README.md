gleam run -m choire
> to update all dependencies


the system resolves around a single object the bought, which has a list of products. so mostly fields are part of the product, only some are for the whole thing.
